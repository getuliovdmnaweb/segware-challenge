import { white_smoke, dark_gray, eclipse } from "../../../utils/colors";

export const drawerOptions = {
  contentContainerStyle: {
    flex: 1,
  },
  labelStyle: {
    fontSize: 16,
    fontWeight: "bold",
  },
  activeTintColor: eclipse,
  inactiveTintColor: dark_gray,
  activeBackgroundColor: white_smoke,
};
